# QoS Graph Lab

Standalone UI experiment for demonstrating before/after adaptive QoS with generated sample data.

## Run

```bash
npm run dev
```

## Build

```bash
npm run build
```

The project is intentionally backend-free. The charts are generated in the browser from a small seeded algorithm so you can click buttons and immediately see the effect of adaptive QoS on bandwidth and delay.
